apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: nifi-app
  namespace: argocd
spec:
  project: apps

  source:
    repoURL: https://github.com/kube-craft/git-ops.git
    targetRevision: HEAD
    path: ./argocd-declarative/manifests/nifi-app

  destination:
    server: https://kubernetes.default.svc
    namespace: nifi

  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      prune: true
      selfHeal: true
