apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: tempo-app
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/kube-craft/git-ops.git
    targetRevision: HEAD
    path: ./argocd-declarative/manifests/tempo-app

  destination:
    server: https://kubernetes.default.svc
    namespace: git-ops

  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      prune: true
      selfHeal: true